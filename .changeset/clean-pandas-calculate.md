---
'dof': minor
---

Add standalone functions for calculating depth of field, aperture, crop factor, and focal length, including typed
inputs and metric defaults. Numeric aperture values now resolve to the nearest named f-stop.
